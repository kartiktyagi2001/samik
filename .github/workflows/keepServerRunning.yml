name: Keep Server Alive

on:
  schedule:
    - cron: "*/10 * * * *" # every 10 minutes (UTC)
  workflow_dispatch: # allows manual run

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render Service
        run: curl -s https://data-forge-50fl.onrender.com/health 
